# 專案摘要｜2026-09-21

## 前次討論與交付

- 已完成繁體中文與英文離線手冊，入口分別為 `Manual/index.html` 與 `Manual/index-en.html`，共用六張實機截圖、樣式與互動程式。
- 頂端語言切換保留閱讀章節，瀏覽器允許時只保存語言偏好。停用 JavaScript 仍可閱讀與透過連結切換。
- 手冊包含 HMI 導覽、B482 路徑範例與警語、設定步驟、FCT／BT 結果對照、八種狀態、異常處理、WeChat 回報範本與開發原理。
- 狀態條件與色碼曾對照原始碼；Session 不會備份原始 Log。前次提交為 `f5f5e31`，已推送 Gitea 與 GitHub。
- 前次完成靜態及模擬互動檢查，實際瀏覽器、手機與列印驗證未完成。

## 本次決議與調整

- 使用者希望 `Manual/` 只含開啟手冊必要檔案，方便整包轉發。
- 將維護文件與前兩份專案摘要移至 `docs/manual/`，保留歷史內容；清除轉發資料夾中的 Finder 中繼資料。
- `Manual/` 保留兩個 HTML、兩個 CSS、一個 JavaScript 與六張原始圖片，共 11 個檔案；中英文同步更新文件日期與維護文件位置。
- 另提供專案根目錄 `B518_Log_Solution_Manual_2026-09-21.zip`，收件者解壓縮後開啟 `Manual/index.html`。ZIP 不含開發紀錄或 macOS 中繼資料，依既有忽略規則不提交 ZIP。
- 本次不修改 App 或手冊互動行為。

## 驗證

- ZIP 完整性檢查通過；解壓縮至暫存資料夾後，11 個檔案與來源逐一比對完全相同。
- 解壓縮版本的兩頁 HTML 資源與章節連結均有效，中英文章節 ID 一致，沒有外部資源引用。
- JavaScript 語法與 Git 差異格式檢查通過。本次未進行實際瀏覽器視覺或互動驗證。
